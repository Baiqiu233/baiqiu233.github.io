<?xml version="1.0" encoding="utf-8"?>
<search>



  <entry>
    <title>测试</title>
    <link href="/2026/06/27/%E6%B5%8B%E8%AF%95/"/>
    <url>/2026/06/27/%E6%B5%8B%E8%AF%95/</url>

    <content type="html"><![CDATA[<p>这是一篇用于测试博客更新流程的文章。</p>
<h2 id="为什么写这篇"><a href="#为什么写这篇" class="headerlink" title="为什么写这篇"></a>为什么写这篇</h2><p>先用一篇很短的文章确认静态博客还能正常更新：页面可以打开，首页能看到入口，归档里能找到记录，搜索索引也能收录内容。</p>
<h2 id="接下来"><a href="#接下来" class="headerlink" title="接下来"></a>接下来</h2><p>如果这篇文章发布顺利，后面就可以把测试内容替换成真正的笔记、教程或者日常记录。最小可用的流程跑通以后，更新博客就会轻松很多。</p>]]></content>



  </entry>
  {% if posts %}
  {% for post in posts.toArray() %}
  {% if post.indexing == undefined or post.indexing %}
  <entry>
    <title>{{ post.title }}</title>
    <link href="{{ [url, post.path] | urlJoin | uriencode }}"/>
    <url>{{ [url, post.path] | urlJoin | uriencode }}</url>
    {% if content %}
    <content type="html"><![CDATA[{{ post.content | noControlChars | safe }}]]></content>
    {% endif %}
    {% if post.categories and post.categories.length>0 %}
    <categories>
      {% for cate in post.categories.toArray() %}
      <category>{{ cate.name }}</category>
      {% endfor %}
    </categories>
    {% endif %}
    {% if post.tags and post.tags.length>0 %}
    <tags>
      {% for tag in post.tags.toArray() %}
      <tag>{{ tag.name }}</tag>
      {% endfor %}
    </tags>
    {% endif %}
  </entry>
  {% endif %}
  {% endfor %}
  {% endif %}
  {% if pages %}
  {% for page in pages.toArray() %}
  {% if post.indexing == undefined or post.indexing %}
  <entry>
    <title>{{ page.title }}</title>
    <link href="{{ [url, post.path] | urlJoin | uriencode }}"/>
    <url>{{ [url, post.path] | urlJoin | uriencode }}</url>
    {% if content %}
    <content type="html"><![CDATA[{{ page.content | noControlChars | safe }}]]></content>
    {% endif %}
  </entry>
  {% endif %}
  {% endfor %}
  {% endif %}
</search>
